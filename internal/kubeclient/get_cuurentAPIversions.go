package kubeclient

// // GetResourceInfos discovers the API resources for a cluster and returns a list of ResourceInfo.
// func (c *KubeClient) GetResourceInfos(name string, kind string, namespace string) ([]ResourceInfo, error) {
// 	resource, err := c.clientset.CoreV1().Pods(namespace).
// }
